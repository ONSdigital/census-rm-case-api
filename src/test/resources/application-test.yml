spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/rm?currentSchema=cases
  cloud:
    gcp:
      pubsub:
        emulator-host: localhost:18539
        project-id: our-project


uacservice:
  connection:
      port: 18165

openapi:
  server-url: http://localhost:8161

springdoc:
  writer-with-order-by-keys: true
  writer-with-default-pretty-printer: true