spring:
  datasource:
    url: jdbc:postgresql://localhost:15433/rm

  cloud:
    gcp:
      pubsub:
        emulator-host: localhost:18539
        project-id: project


uacservice:
  connection:
      port: 18165
